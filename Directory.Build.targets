<Project>
  <PropertyGroup Condition="'$(PlatformTarget)' == 'x86'">
    <RunCommand>$(MSBuildProgramFiles32)\dotnet\dotnet</RunCommand> <!-- Support XNA 32bit debugging in VS -->
  </PropertyGroup>
</Project>